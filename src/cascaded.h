#ifndef __CASCADED_H
#define __CASCADED_H

#include <stdio.h>
#include <stdlib.h>

int cascade(char s[]);
int two_channel_cascade(char s[]);
size_t bufsize;
int int_bufsize;
uint32_t uint_bufsize;
void reverse(char s[]);
void itoa(int n, char s[]);
#endif
